<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	<application
        android:allowBackup="true"
        android:icon="@mipmap/appicon"
        android:roundIcon="@mipmap/appicon_round"
        android:supportsRtl="true">

		<meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBikk1CUzHGvaT4d8uPby3Gy67gBCCuIFA" />

		<!-- Intent filter para abrir arquivos .kml -->
		<activity
            android:name=".MainActivity"
            android:label="Terra Marcada"
            android:launchMode="singleTask"
            android:exported="true">
			<intent-filter>
				<!-- Ação para abrir arquivos -->
				<action android:name="android.intent.action.VIEW" />

				<!-- Categoria padrão para visualização -->
				<category android:name="android.intent.category.DEFAULT" />
				<category android:name="android.intent.category.BROWSABLE" />

				<!-- Tipo MIME para arquivos .kml -->
				<data android:mimeType="application/vnd.google-earth.kml+xml" />

				<!-- Para abrir arquivos locais (arquivos no dispositivo) -->
				<data android:scheme="file" />
			</intent-filter>
		</activity>
	</application>

	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
	<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
	<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
	<uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS" />
	<uses-permission android:name="android.permission.CAMERA" />
	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
	<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
	<uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />

</manifest>
